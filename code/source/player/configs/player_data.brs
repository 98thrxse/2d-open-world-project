function player_data(object as object)

    control_options = {
        
        direction: {
            x: invalid,
            y: invalid
        },
    
        collider: {
            up: invalid,
            down: invalid
            left: invalid,
            right: invalid
        }
    
        speed: {
            up: invalid,
            down: invalid,
            left: invalid,
            right: invalid
        }
    }

    return control_options

end function